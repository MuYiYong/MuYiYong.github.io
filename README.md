# AboutMe

静态个人主页项目。

## 文件说明

- `index.html`：主页结构与双语内容
- `styles.css`：页面样式、区块层次与响应式布局
- `script.js`：语言切换、导航高亮与平滑滚动

## 本地预览

直接在浏览器中打开 `index.html` 即可预览。

如需通过本地静态服务访问，可在当前目录执行：

```bash
python3 -m http.server 8080
```

访问 `http://localhost:8080`。

## 维护说明

- 当前仓库仅包含主页展示所需的静态资源与脚本。

